{
  "name": "root",
  "private": true,
  "scripts": {
    "run:products-web": "lerna run --scope products-web dev",
    "run:server": "lerna run --scope server start:dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.1"
  }
}
